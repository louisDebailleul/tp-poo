{% extends 'layout.twig' %}

{% block title "Mes articles" %}

{% block body %}
  <h1>Bienvenue sur les meeting</h1>
  <table class="table table-striped table-dark" >
      <thead>
      <tr>
          <td scope="col">Titre</td>
          <td scope="col">date debut</td>
          <td scope="col">date fin</td>
          <td scope="col">createur du meeting</td>
          <td scope="col"></td>

      </tr>
      </thead>
      {% for meeting in meetings %}
        <tr>
      <td>

          {{ meeting.nom }}
        </a>
      </td>
            <td>
                {{ meeting.dateD }}
            </td>
            <td>
                {{ meeting.dateF }}
            </td>
            <td>
                {{ meeting.email }}
            </td>
            <td>
            <a href="{{ path('meeting.show', {slug: meeting.slug, id: meeting.id}) }}">
                en savoir plus
            </a>
            </td>
        </tr>
    {% endfor %}

  </table>
{% endblock %}
